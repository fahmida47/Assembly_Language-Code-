.MODEL SMALL
.STACK 100H
.DATA
ARR DB 1,2,3,4,5,6 

.CODE
MAIN PROC
    MOV AX, @DATA
    MOV DS, AX
    
    MOV SI, 0
    MOV DI, 0
    XOR CX, CX
    XOR BX, BX
    MOV CX, 3
;MOV DI, ARR_SIZE
    MOV DI, 6
    DEC DI ;di = 5
REVERSE:
    MOV BL, ARR[SI] ;1
    MOV BH, ARR[DI] ;6
    MOV ARR[SI], BH ;6
    MOV ARR[DI], BL ;1
    INC SI
    DEC DI
    LOOP REVERSE  

  
END_:
MOV AH, 4CH
INT 21H    
MAIN ENDP
INCLUDE INDEC.ASM
INCLUDE OUTDEC.ASM
END MAIN

